/**
 * Qovrion has no published update channel yet. Keep the component boundary so
 * the shell does not need a broad rewrite, but never render an upstream nudge.
 */
export function UpdateBanner() {
  return null
}
